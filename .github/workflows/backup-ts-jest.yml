name: Backup TS Jest

on:
  schedule:
    - cron: '0 23 * * *' # Run every day at 11 PM
  workflow_dispatch:

env:
  NODE_VERSION: '18'
  POSTGRES_VERSION: '15'

jobs:
  backup-ts-jest:
    name: Backup TS Jest
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Backup ts-jest
        run: |
          echo "Backing up ts-jest..."
          # Add ts-jest backup logic here
          echo "TS Jest backup completed ✅"

  backup-vitest:
    name: Backup Vitest
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Backup vitest
        run: |
          echo "Backing up vitest..."
          # Add vitest backup logic here
          echo "Vitest backup completed ✅"
