name: Backup Package

on:
  schedule:
    - cron: '0 18 * * *' # Run every day at 6 PM
  workflow_dispatch:

env:
  NODE_VERSION: '18'
  POSTGRES_VERSION: '15'

jobs:
  backup-package:
    name: Backup Package
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Backup package
        run: |
          echo "Backing up package..."
          # Add package backup logic here
          echo "Package backup completed ✅"

  backup-package-lock:
    name: Backup Package Lock
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Backup package-lock
        run: |
          echo "Backing up package-lock..."
          # Add package-lock backup logic here
          echo "Package lock backup completed ✅"
