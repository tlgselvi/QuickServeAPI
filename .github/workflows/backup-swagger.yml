name: Backup Swagger

on:
  schedule:
    - cron: '0 12 * * *' # Run every day at 12 PM
  workflow_dispatch:

env:
  NODE_VERSION: '18'
  POSTGRES_VERSION: '15'

jobs:
  backup-swagger:
    name: Backup Swagger
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Backup swagger
        run: |
          echo "Backing up swagger..."
          # Add swagger backup logic here
          echo "Swagger backup completed ✅"

  backup-axios:
    name: Backup Axios
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Backup axios
        run: |
          echo "Backing up axios..."
          # Add axios backup logic here
          echo "Axios backup completed ✅"
