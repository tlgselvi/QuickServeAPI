name: Backup Concurrently

on:
  schedule:
    - cron: '0 20 * * *' # Run every day at 8 PM
  workflow_dispatch:

env:
  NODE_VERSION: '18'
  POSTGRES_VERSION: '15'

jobs:
  backup-concurrently:
    name: Backup Concurrently
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Backup concurrently
        run: |
          echo "Backing up concurrently..."
          # Add concurrently backup logic here
          echo "Concurrently backup completed ✅"

  backup-nodemon:
    name: Backup Nodemon
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Backup nodemon
        run: |
          echo "Backing up nodemon..."
          # Add nodemon backup logic here
          echo "Nodemon backup completed ✅"
