name: Backup Body Parser

on:
  schedule:
    - cron: '0 14 * * *' # Run every day at 2 PM
  workflow_dispatch:

env:
  NODE_VERSION: '18'
  POSTGRES_VERSION: '15'

jobs:
  backup-body-parser:
    name: Backup Body Parser
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Backup body-parser
        run: |
          echo "Backing up body-parser..."
          # Add body-parser backup logic here
          echo "Body Parser backup completed ✅"

  backup-reflect-metadata:
    name: Backup Reflect Metadata
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Backup reflect-metadata
        run: |
          echo "Backing up reflect-metadata..."
          # Add reflect-metadata backup logic here
          echo "Reflect Metadata backup completed ✅"
